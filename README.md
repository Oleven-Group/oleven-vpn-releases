# Oleven VPN — release

APK firmati (Certum, `CN=Giuseppe Vaccarella`) e manifest per l'auto-update dell'app.
Il **sorgente resta privato**; qui stanno solo i binari firmati (nessun segreto).

- `version.json` — manifest letto dall'app per gli aggiornamenti automatici.
- Release → gli APK scaricabili.
